---
name: Warmup Service
on:
  schedule:
    - cron: "*/12 * * * *"
jobs:
  run-updater:
    runs-on: ubuntu-latest
    steps:
      - name: REST API with curl
        run: |
          curl -s "https://tappaevents.onrender.com/events"